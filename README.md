# calculator_service_advanced :mortar_board: :triangular_ruler:
***
# 📚Распределённый вычислитель арифметических выражений

Веб-сервис, который конкурентно вычисляет значение отправленного пользователем по HTTP арифметического выражения и отправляет пользователю результат.  

Данный калькулятор поддерживает такие арифметические операции как: ➕, ➖, ➗, ✖️. Также поддерживает приоритизацию действий, включая скобки ().

Текущая версия сервиса поддерживает работу с отрицательными числами. Использование знаков + или - перед числами также допустимо✅(в отличие от [предыдущей версии проекта](https://github.com/DariaYurch/calculator_service))
***
# 🚀Запуск
Для запуска сервиса необходимо полносью клонировать репозиторий [calculator_service_advanced](https://github.com/DariaYurch/calculator_service_advanced) и запустить оркестратор и агент (запускаются одной командой, но открывается два терминала) ```go cmd/main.go```  

Клонирование репозитория:  
``` 
git clone https://github.com/DariaYurch/calculator_service_advanced
```
Запуск сервера:  
```
go cmd/main.go
```
Один из терминалов - терминал оркестратора, другой - терминал агента.
***
# 👨‍💻Руководство пользователя
У сервиса 3 основных endpoint со следующими url-ами:
1) ```localhost:8080/api/v1/calculate``` - Добавление вычисления арифметического выражения    
Пользователю необходимо отправить на этот url POST-запрос с телом:
```
{
    "expression": "выражение, которое ввёл пользователь"
}
```
В ответ пользователь получает HTTP-ответ с телом:
```
{
    "id": "сгенерированный id выражения"
}
```
и кодом 201, если выражение валидно и записано в БД, либо ошибка с текстом:
```
Error: The expression contains invalid symbols
```
и кодом 422, если входные данные не соответствуют требованиям приложения — например, кроме цифр и разрешённых операций пользователь ввёл символ английского алфавита.  
Ещё один вариант текста ошибки:
```
Expression can not be empty
```
Если пользователь передает пустое выражение.   
И еще один вариант текста ошибки:
```
Error generating the id
```
и код 500 в случае какой-либо иной ошибки (например, ошибка генерации ID для выражения).   
2) ```localhost:8080/api/v1/expressions``` - Получение списка выражений   
Пользователю необходимо отправить на этот url GET-запрос без тела.
В ответ пользователь получает HTTP-ответ с телом:
```
{
    "expressions": [
        {
            "id": <идентификатор выражения>,
            "status": <статус вычисления выражения>,
            "result": <результат выражения>
        },
        {
            "id": <идентификатор выражения>,
            "status": <статус вычисления выражения>,
            "result": <результат выражения>
        }
    ]
}
```
и кодом 200, если успешно получен список выражений, либо ошибка с текстом:
```
No expressions found
```
и кодом 500, если произошла какая-либо ошибка (например, пустая БД или ошибка подключения к БД)   
3)```localhost:8080/api/v1/expressions/id``` - Получение выражения по его идентификатору.   
Пользователю необходимо предварительно сохранить ID выражения, полученный при операции "Добавление вычисления арифметического выражения" и отправить на url с id GET-запрос без тела.   
Пример верного GET-запроса:
```
localhost:8080/api/v1/expressions/c9a075b5b96b5846
```
В ответ пользователь получает HTTP-ответ с телом:
```
{
    "expression":
        {
            "id": <идентификатор выражения>,
            "status": <статус вычисления выражения>,
            "result": <результат выражения>
        }
}
```
и кодом 200, если успешно получена задача, либо ошибка с текстом:
```
Expression not found
```
и кодом 404, если выражение не найдено.   
Еще один вариант текста ошибки:
```
Invalid id
```
и код 500, если введенный ID невалидный.   
Также сервис имеет два внутренних endpoint для общения между собой оркестратора и агента с url-ами:
1) ```localhost:8080/internal/task ``` - Получение задачи для выполнения (GET-запрос)
Данный endpoint позволяет агенту брать задачи от оркестратора. Тело ответа:
```
{
    "task":
        {
            "id": <идентификатор задачи>,
            "arg1": <имя первого аргумента>,
            "arg2": <имя второго аргумента>,
            "operation": <операция>,
            "operation_time": <время выполнения операции>
        }
}
```
Возможные коды ответа:
- 200 - успешно получена задача
- 404 - нет задачи
- 500 - что-то пошло не так
2) ```localhost:8080/internal/task ``` - Прием результата обработки данных (POST-запрос)
Данный endpoint позволяет агенту посылать результат выражения оркестратору. Тело запроса:
```
{
  "id": "c9a075b5b96b5846",
  "result": 2.5
}
```
В данном примере выражению с ID ```c9a075b5b96b5846``` присваивается результат ```2.5```.   
Возможные коды ответа:
- 200 - успешно записан результат
- 404 - нет такой задачи
- 422 - невалидные данные
- 500 - что-то пошло не так

***
# 🔢Примеры использования
Перед началом работы необходимо создать БД. Название БД, имя пользователя, пароль (и другие настройки проекта) необходимо прописать в файле ```.env```. В репозитории есть пример такого файла - ```.env.example``` (можно просто его скопировать и ввести свои данные).   
Для создания таблиц в БД можно использовать файл ```schema.sql``` в директории ```db/schema.sql```.
## Пример работы сервиса без ошибок✅:
Пример работы сервиса для вычисления арифметического выражения ``` (3 + 8) * 2 + 8 ```:  
Отправляем POST-запрос на endpoint с url-ом ```localhost:8080/api/v1/calculate```:
```
curl --location 'localhost:8080/api/v1/calculate' \
--header 'Content-Type: application/json' \
--data '{
    "expression": "(3 + 8) * 2 + 8"
}'
```
После чего получаем сгенерированный ID заданного выражение и код состояния ответа HTTP 201 (Created)🟢
```
{
    "id":"2ec6dd79bfa8e541"
}
```
Также можно посмотреть результат этого выражения, отправив GET-запрос на endpoint с url-ом ```localhost/api/v1/expressions/2ec6dd79bfa8e541```   
После чего получаем статус выражения, его результат и код состояния ответа HTTP 200 (OK)🟢
```
{
    "expression": [
        {
            "id": "2ec6dd79bfa8e541",
            "status": "COMPLETED",
            "result": 30
        }
    ]
}
```
Если добавить еще несколько выражений, то можно запросить их все одним GET-запросом на endpoint с url-ом ```localhost/api/v1/expressions```   
После чего получаем информацию по всем существующим в БД выражениям и код состояния ответа HTTP 200 (OK)🟢
```
{
    "expressions": [
        {
            "id": "2ec6dd79bfa8e541",
            "status": "COMPLETED",
            "result": 30
        },
        {
            "id": "3be2981b86976172",
            "status": "COMPLETED",
            "result": 72
        },
        {
            "id": "5cae215bbddeb763",
            "status": "COMPLETED",
            "result": 6
        },
        {
            "id": "c9a075b5b96b5846",
            "status": "COMPLETED",
            "result": 6
        }
    ]
}
```
## Пример работы сервиса с ошибками ⚠️
1) Работа с endpoint ```localhost:8080/api/v1/calculate```   
- Отправка некорректного выражения ```1 + a```:   
   POST-запрос:
   ```
    {
    "expression": "1 + a"
    }
   ```
   После чего получаем ошибку с текстовым описанием и код состояния ответа HTTP 422 (Unprocessable Entity)🔴
   ```
   Error: The expression contains invalid symbols
   ```
- Отправка пустого выражения:   
  РОST-запрос:
  ```
    {
    "expression": ""
    }
  ```
  После чего получаем ошибку с текстовым описанием и код состояния ответа HTTP 422 (Unprocessable Entity)🔴
  ```
  Expression can not be empty
  ```
2) Работа с endpoint ```localhost:8080/api/v1/expressions```
- Запрос выражений из пустой БД:   
  При попытке получить все выражения из пустой БД получаем ошибку с текстовым описанием и код состояния ответа HTTP 500 (Internal Server Error)🔴
  ```
  No expressions found
  ```
3) Работа с endpoint ```localhost:8080/api/v1/expressions/id'''
- Запрос несуществующего выражения:   
  GET-запрос:
  ```
  localhost:8080/api/v1/expressions/123task
  ```
  После чего получаем ошибку с текстовым описанием и код состояния ответа HTTP 404 (Not Found)🔴
  ```
  Expression not found
  ```
- Некорректный ID:  
     GET-запрос:   
     ```
     localhost:8080/api/v1/expressions/123task
     ```
     После чего получаем ошибку с текстовым описанием и код состояния ответа HTTP 500 (Internal Server Error)🔴
     ```
     Invalid id
     ```
